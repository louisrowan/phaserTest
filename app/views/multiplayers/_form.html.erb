<%= form_for(multiplayer) do |f| %>
  <% if multiplayer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(multiplayer.errors.count, "error") %> prohibited this multiplayer from being saved:</h2>

      <ul>
      <% multiplayer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
